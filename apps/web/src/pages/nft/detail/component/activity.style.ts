import { styled } from 'styled-components'

export const Wrapper = styled.div`
  .sgt-offer__wrapper {
    margin-top: 12px;
  }
  .sgt-offer__icon {
    margin-left: 6px;
    width: 20px;
    height: 20px;
  }
  .sgt-offer__tag {
    box-sizing: border-box;
    display: flex;
    flex-direction: row;
    align-items: center;
    padding: 0 8px;
    height: 28px;
    border-radius: 16px;
    background-color: rgba(5, 201, 154, 0.12);
  }
  .sgt-offer__tag-content {
    font-size: 16px;
    font-weight: 600;
    color: rgba(5, 201, 154, 1);
  }
`
