import React from 'react'
import styled from 'styled-components'
import { AddIcon, IconButton } from '@pancakeswap-libs/uikit'

const ApyButton: React.FC = () => {
  return (
    <IconButton variant="text" size="sm" ml="4px">
      <AddIcon />
    </IconButton>
  )
}

export default ApyButton
