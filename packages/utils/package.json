{
  "name": "@pancakeswap/utils",
  "version": "0.0.1",
  "scripts": {
    "test": "vitest --run --globals"
  },
  "private": true,
  "devDependencies": {
    "ethers": "^5.0.0",
    "@pancakeswap/swap-sdk-core": "workspace:*"
  }
}
