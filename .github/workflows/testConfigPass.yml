name: Test Config Pass

on:
  pull_request:
    paths-ignore:
      - 'apps/web/src/config/**'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        node: [16]
    name: Config (Jest)
    steps:
      - run: 'echo "No Test Config required" '
