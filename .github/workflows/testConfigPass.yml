name: Test Config Pass

on:
  pull_request:
    paths-ignore:
      - 'src/config/**'

jobs:
  test:
    runs-on: ubuntu-latest
    name: Config (Jest)
    steps:
      - run: 'echo "No Test Config required" '
