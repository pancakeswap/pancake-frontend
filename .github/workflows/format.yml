name: Format

on:
  pull_request:

jobs:
  code:
    runs-on: ubuntu-latest
    timeout-minutes: 6

    steps:
      - name: Install dependencies
        uses: './.github/actions/install-deps'

      - name: Run Prettier
        run: pnpm format:check
